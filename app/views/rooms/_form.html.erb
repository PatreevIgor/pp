<%= form_for(@room) do |f| %>
  <% if @room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% @room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    Select_User_to_room_invite: <br>

  <% @users.each do |user| %> 
    <%= user.name %> 
    <%= check_box_tag "check_box[#{user.name}]" %> 
  <% end %> 

  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

